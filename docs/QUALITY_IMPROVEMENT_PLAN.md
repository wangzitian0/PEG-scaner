# 工程质量加强计划

基于 agent.md (41-48) 新要求

---

## 📋 新增要求分析

### Line 43: 利用存量
- ✅ 已做到：本次重构使用了core/schemas/，没有重写
- ✅ 已做到：复用了data_collection/代码

### Line 44: Linux准则
- ❌ 待改进：根目录文件太多
- ❌ 待改进：缺少x-开头的文件夹

### Line 45: 测试+数据校验
- ✅ 已做到：46/46 tests passed
- ⚠️ 待加强：需要添加数据产物校验

### Line 46: 目录管理 ⚠️ **重点**
- ❌ **问题**：根目录10个目录+9个文件（超标！）
- ✅ 目标：6-7个目录+3-4个文件

### Line 47: append_prompt.md
- ❌ 待创建：需要创建append_prompt.md追溯提示词

### Line 48: x-开头文件夹
- ❌ 待创建：x-log/, x-data/等

---

## 🎯 改进计划

### 1. 重新组织根目录（Line 46优先级最高）

当前根目录（10目录+9文件）：
```
目录：
- backtest/
- core/
- data/
- data_collection/
- docs/
- htmlcov/
- tests/
- .git/
- .venv/
- .pytest_cache/

文件：
- README.md
- agent.md
- pyproject.toml
- .gitignore
- .project-structure.md
- LICENSE
- config.yaml
- uv.lock
- .coverage
```

**改进方案**：

保留的核心目录（6个）：
1. `src/` - 所有代码（合并core, data_collection, backtest）
2. `tests/` - 测试
3. `docs/` - 文档
4. `x-data/` - 程序生成的数据（替代data/）
5. `x-log/` - 程序生成的日志
6. `.git/`, `.venv/`, `.pytest_cache/` 等系统目录（隐藏）

保留的核心文件（4个）：
1. `README.md` - 主README
2. `agent.md` - 主指令
3. `pyproject.toml` - 项目配置
4. `.gitignore` - Git配置

移动或整合：
- `data/` → `x-data/`（程序生成）
- `htmlcov/` → `x-coverage/`或删除
- `.project-structure.md` → `docs/`
- `config.yaml` → `src/config.yaml`或保留
- `LICENSE` → `docs/LICENSE`或保留
- `.coverage` → 隐藏文件，保留

### 2. 创建x-开头文件夹（Line 48）

```
x-data/          # 程序生成的数据（原data/）
x-log/           # 程序生成的日志
x-coverage/      # 测试覆盖率报告（原htmlcov/）
```

### 3. 创建append_prompt.md（Line 47）

记录所有后续追加的提示词

### 4. 添加数据产物校验（Line 45）

创建：
- `tests/test_data_quality.py` - 数据质量测试
- 校验CSV文件格式
- 校验命名规范
- 校验Schema一致性

---

## 🚀 执行顺序

1. ✅ 创建x-开头文件夹
2. ✅ 移动data/ → x-data/
3. ✅ 移动htmlcov/ → x-coverage/
4. ✅ 整理文档到docs/
5. ✅ 更新所有引用路径
6. ✅ 创建append_prompt.md
7. ✅ 添加数据质量测试
8. ✅ 运行完整测试
9. ✅ 更新README索引

