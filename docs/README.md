# Docs Index

This repository now follows the structured layout described in `docs/index.md`. Start there for the value index and navigation. Keep all new documents aligned with the `origin/`, `specs/`, and `project/` blocks.
