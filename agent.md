
帮我写一个 readme<科技公司最重要的估值指标是PEG>
# 基本要求
- 使用markdown格式。可以使用**LaTeX 标准格式**，可以使用```mermaid```。
- 不超过 5000 字。
- 涉及到数据，此时此刻=请基于昨天的收盘价（2025年11月14号）。
- 公司名请使用格式`腾讯<00700.hk>`，`微软<msft.us>`。
- 表格应当行跟着目标变，列始终为：利润，利润增速，TTM PE，PEG。
# 章节说明
## 原理解释
- 数学原理与合理性，自由现金流，泰勒展开第一项等多视角说明。
- 综合使用各种格式，让内容容易懂。
- 分析腾讯，亚马逊，微软。
## 策略回测
- 制作一个回测工具(初始版本使用腾讯，微软，亚马逊)表格: 2000-2025对应的每个月开盘价的 PEG，每次 PEG 低于 X 买入，高于 Y 卖出，年化收益率是多少
- SP500，vgt，kweb拉列表，分别回测
## 策略计算
- 制作1个此时此刻 PEG 的表格：美股七姐妹+港股七姐妹
- 我想同时覆盖中美科技公司，最好的选项就是vgt+kweb，请完整获取列表。
- 筛选条件为：利润一千万美金以上的公司，peg最低的15家。
- 结果仅展示一个15家公司此时此刻的PEG的表格


# 一些细节要求
## 数据管理
- 因为是重度的数据应用，请你将 schema 写下来放在core里面的某个文件夹
- 拉数据为了保证置信度，应当使用至少三个免费的财经数据源
- 所有采用的数据，必须至少有两个数据源且相同，才进行下一步
- 宁可为空，不要使用错的数据
- 本质相同但是不同来源的数据(你加工之后的置信数据也可以认为是一个source)应当遵循 schema 且请放到一个文件夹
- 命名规则为 schema-name-source-date.csv -> etf_portfolio-vgt-yfinance-20240101_20251114.csv
## 代码管理
- 整个项目使用uv 管理，相关代码全部使用 python
- 数据表格使用 markdown 或者 csv 方便读写
- 数据处理的中间过程尽可能持久化，这样好 debug
- 最大化的遵循 SSOT 原则，本质相同的东西放到一个文件夹或者一个文件。
## 项目管理
- 每个目录都要有字节的 readme.md。
- 每次改动都要修改相关目录的 readme.md，从对应的文件改到根目录。更上层的 readme 应该是包含子目录的索引。
- 项目类宏观层面的进度请放在 docs/readme.md，能够 onepage 知道现在做到哪里了。
- 微观层面的迭代每个 phrase 请放在一个文件夹，命名为phrase_i.xxxx，里面放对应的 plan,迭代流程，checklist各种 md 文件
## 工程优化准则
- 每次迭代前，都要尽可能利用存量的文档和代码。不要上来就写一个全新的然后删掉。
- 尽可能遵循 Linux 写东西的基本准则
- 每次改代码都要跑测试
- 请严格管理目录，符合人类阅读习惯的组织结构就是 6～7 个目录+ 3～4 个文件。打破了就要重新组织。